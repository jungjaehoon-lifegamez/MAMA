# MAMA OS 0.51.2 hotfix evidence

Date: 2026-09-08. Scenarios: TG-01/TG-06.
Base: a9402683571e847180326b1ff1c1765a1c108460. Candidate: the commit containing this artifact.

## Problem and correction

A board turn is instructed to publish three judgment slots. The pipeline is a host-managed task
projection. The full-repair receipt query incorrectly required pipeline in the same model publish
receipt, leaving repair dirt after successful work. A later scheduled turn redundantly checked it.

The query now requires the three judgment slots, retaining successful execution, channel, attempt,
trace watermark and persisted-slot checks. Full-board freshness still includes the pipeline.
No prompt, model, schema, authority, scheduling or delivery policy changes are included.

## Reproduction and verification

- Baseline regression: `MAMA_FORCE_TIER_3=true pnpm exec vitest run tests/operator/workorder-hooks.test.ts -t 'accepts persisted judgment'`
  from standalone failed at the expected receipt assertion (`false` versus `true`).
- Candidate focused suite: workorder-hooks, board-refresh-gate, report-freshness, workorder-consumer:
  112 tests passed. Real executor -> publisher/store -> SQLite receipt -> repair gate is exercised.
- Read-only historical trace replay with the built candidate: old four-slot predicate matched0;
  corrected same-attempt query matched1. No operating data was changed.
- `pnpm test`: exit0, 7 successful tasks; standalone5723 passed,7 skipped.
- `pnpm build`: exit0, 2 successful tasks. Changed-file ESLint and standalone typecheck passed.
- `pnpm sync-versions --check`: passed. Only mama-os is version-bumped/published.

## Independent review

A separate Codex agent reviewed the implementation, producer contract, full-board freshness and
reproduction fidelity. PASS, no blocking findings. It independently ran workorder-hooks21/21.
It did not independently replay the production database; that is primary-agent evidence.

## Boundaries

Fixtures use in-memory DB/store and supplied model decisions/context. They omit full daemon boot,
transport ingress, backend model, scheduling, SSE/browser and Telegram delivery. Restart persistence
is not exercised by the fixture. Historical replay tests the observed predicate mismatch directly.
These checks support this narrow execution-verification fix, not judgment quality, latency targets
or the broader owner-runtime redesign. Installation/restart checks follow publication separately.

## Content fingerprints

- Source SHA256: `377e828872d443f58631aedc6a1c2fd0756b652aa1fe102c5c405cec4a7051c2`
- Regression fixture SHA256: `7025b711031100416c04a175becfa0754a0d94e32c6114f6e6bca90aa04c108b`
