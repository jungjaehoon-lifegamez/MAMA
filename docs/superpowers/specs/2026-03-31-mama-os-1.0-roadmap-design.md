# MAMA OS 1.0 Roadmap Design Spec

## Overview

MAMA OS evolves from a multi-agent dashboard into a **local AI runtime platform** with connected memory. The core value proposition: your digital life, remembered locally, connected intelligently, always yours.

This document defines the phased roadmap from v0.15 (current) to v1.0 (general release).

## Strategic Context

### What Claude/LLMs Commoditized

- Multi-agent orchestration (sub-agent dispatch)
- Skills/plugins marketplace
- Code execution sandbox
- MCP server integration

### What MAMA OS Uniquely Provides

- **Local persistent memory** with evolution tracking (supersedes/builds_on/debates)
- **Always-on daemon** (watches, learns, acts without user prompting)
- **Messenger-native connectivity** (Kakao, LINE, Slack, Telegram, Discord)
- **AI provider independence** (memory outlives any AI service)
- **Cross-source intelligence** (code decisions + team chat + email = one graph)

### Competitive Landscape

| Competitor  | Model                                         | MAMA OS Differentiation                                       |
| ----------- | --------------------------------------------- | ------------------------------------------------------------- |
| Supermemory | Cloud API, closed source, #1 benchmarks       | Local-only, open source, messenger-native, evolution tracking |
| Mem0        | Cloud API ($24M YC), OpenMemory MCP for local | Local-first from day one, app ecosystem, not just SDK         |
| Pieces      | Local developer tool, 9-month memory          | Developer-only, no messenger/calendar/email, no app ecosystem |
| Reor        | Local note app, semantic search               | Note app only, no external app connection, manual input       |

**MAMA OS occupies a unique position: local + always-on + app ecosystem + messenger-native.** No competitor combines all four.

### Why MAMA OS, Not Just an MCP Server

This is the most important architectural distinction.

```
MCP Server:  Claude calls mama_save → saves one fact → done
             Claude calls mama_search → returns results → done
             (Tool. Works when called. Silent otherwise.)

MAMA OS:     Always running on localhost:3847
             Receives hook events from Claude Code Plugin
             Memory agent analyzes conversations automatically
             Connectors poll messengers continuously
             Supervisor manages app lifecycles
             (Runtime. Watches, learns, acts. Always on.)
```

MCP is a tool that Claude picks up and puts down. MAMA OS is a runtime that runs whether Claude is active or not. This difference enables:

1. **Automatic memory extraction** — Claude Code Plugin hooks fire on every user message (`UserPromptSubmit`) and tool call (`PostToolUse`). MAMA OS receives these and the memory agent decides what to save. No explicit `mama_save` needed.

2. **Cross-session continuity** — when a new Claude Code session starts, MAMA OS's hook injects relevant memory from previous sessions. The user never has to say "remember last time."

3. **Background intelligence** — while you code, MAMA OS processes incoming Slack messages, detects related context, and surfaces it in the next hook. You didn't ask for it. It's just there.

4. **Graceful degradation** — if MAMA OS is off, Claude Code still works. Hooks fail silently. When MAMA OS starts again, it picks up where it left off.

No MCP server can do this. MCP servers are stateless tools invoked on demand. MAMA OS is a stateful runtime that observes, learns, and provides context continuously.

### Connector-Orchestrator Architecture (decided 2026-04-01)

MAMA OS adopts a **2-tier intelligence model**:

```text
Tier 1 — Connectors (domain-specialized extraction agents)
  Each connector: delta polling + raw archive + domain-specific extraction
  Raw stays in the connector (transcript files, channel DBs, API caches)
  Only structured memory units flow to MAMA OS

Tier 2 — MAMA OS Orchestrator (cross-connector reasoning)
  Receives structured units from all connectors
  Cross-connector pattern detection, contradiction resolution
  Temporal pattern analysis ("this decision flipped 3 times")
  Knowledge Graph = single source of truth (no raw duplication)
  Evidence pointers reference connector raw data when verification needed
```

**Key insight from Kagemusha experience:** delta-only analysis (looking at "what changed now") lacks cross-channel and temporal insight. MAMA OS must reason over the full Knowledge Graph, not just the latest delta. Raw archives enable verification but the Knowledge Graph must be sufficient for 99% of queries.

### Existing Connectors

| Connector              | Raw Archive                                        | Extraction Agent                                                                     | Scope                   |
| ---------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------- |
| Claude Code Plugin     | `transcript_path` (.jsonl, managed by Claude Code) | Hooks: UserPromptSubmit (transcript), PostToolUse (edit batch), PreCompact (session) | `claude-code/{project}` |
| MCP Server             | MCP session state                                  | Direct `mama_save` calls                                                             | `mcp-server/{session}`  |
| MAMA OS Gateways       | Channel message history                            | Gateway message router                                                               | `gateway/{platform}`    |
| Kagemusha (mama-suite) | `kagemusha.db` (messages, rooms, FTS5)             | AgentAwareness (batch digest)                                                        | `kagemusha/{channel}`   |

All feed mama-core's Knowledge Graph. The 1.0 roadmap formalizes this into a Connector SDK.

## Target Users

| Phase      | Primary User                                           |
| ---------- | ------------------------------------------------------ |
| v0.16-0.17 | Self + Kagemusha (dogfooding)                          |
| v0.18-0.19 | Developer community (Claude Code users)                |
| v0.20-1.0  | Non-developers (UI-driven setup, no terminal required) |

## 1.0 Definition of Done

MAMA OS 1.0 is reached when:

1. Memory quality is proven (benchmark >60% on 100 questions)
2. App ecosystem works (connector framework + 2+ connectors)
3. Stability is guaranteed (watchdog, audit log, 72h uptime test)
4. Non-developers can install and use it (UI onboarding, <5 min setup)

---

## Phase 1: v0.16 — Memory Engine Completion

**Goal:** mama-core becomes a complete memory platform, not just a decision store.

### 1.1 ingestConversation Hybrid Extraction

Migrate hybrid extraction from test scripts into mama-core API.

```typescript
await mama.ingestConversation({
  messages: [...],
  scopes: [{ kind: "app", id: "slack" }],
  extract: {
    enabled: true,
    mode: "hybrid",      // code regex first, LLM supplement
    codeThreshold: 2,    // LLM when code finds < 2 facts
    batchSize: 3,        // sessions per LLM call
  }
})
```

Pipeline:

1. Code regex extraction (15+ patterns, instant, free)
2. LLM supplement for sessions with < 2 code facts (batched, persistent session)
3. Entity key generation (unique per fact, no supersedes collision)
4. Scope tagging + topic classification
5. Supersedes chain detection (same entity across sessions)

### 1.2 ingestDocument API

New API for non-conversational data.

```typescript
await mama.ingestDocument({
  type: 'image' | 'pdf' | 'spreadsheet' | 'document',
  source: { app: 'gmail', id: 'attachment_123' },
  content: 'extracted text or LLM vision description',
  metadata: { filename: 'contract.pdf', size: 1234 },
  scopes: [{ kind: 'app', id: 'gmail' }],
});
```

mama-core stores extracted facts only, never raw files. Connectors handle extraction, mama-core handles storage + search.

### 1.3 Memory Agent — Real-Time Observation + Extraction ✅ IMPLEMENTED

The memory agent watches live conversations (Claude Code sessions, messenger chats) and automatically saves important content without explicit user commands.

**Claude Code Connector pipeline (verified 2026-04-01):**

```
Claude Code session
       ↓ UserPromptSubmit hook fires
  reads transcript_path (.jsonl) → extracts last 5 turns (user + assistant)
       ↓ POST /api/memory-agent/ingest (fire-and-forget)
  MAMA OS receives → PersistentCLI extraction (sonnet, no API key)
       ↓ extraction prompt (SW dev specialized)
  Structured units (decision/preference/fact/lesson/constraint)
       ↓ noise filter + topic matching + evolution edge detection
  mama-core Knowledge Graph
       ↓ auto-recalled in next session via SessionStart hook
```

**Additional capture points:**

- PostToolUse (Edit/Write) → batched with 3-edit debounce, flushed on next UserPromptSubmit
- PreCompact → full transcript + unsaved decision detection before context compression

**Extraction prompt key rule:** User statements = decisions ("~하자", "let's use X"). Assistant statements = context (NOT decisions). Greetings, task-notifications, meta-conversation skipped.

**Verified E2E:** User said "PostgreSQL로 전환하자" → extracted as "User decided to switch from better-sqlite3 to PostgreSQL, reversing the previous decision" with correct topic linking.

**This is MAMA OS's killer use case.** No competitor has an agent that observes your coding sessions and automatically builds a memory graph. Supermemory and Mem0 provide APIs — they don't watch and learn.

### 1.4 Scope-Based Vector Search

Upgrade from topicPrefix string matching to proper scope-based filtering.

```typescript
// Isolated: connector sees only its data
mama.search('meeting schedule', { scopes: [{ kind: 'app', id: 'slack' }] });

// Cross-scope: user sees everything
mama.search('client A', { scopes: 'all' });
// → Slack 3 results + Telegram 2 + Gmail 1, unified ranking
```

Implementation: extend the topicPrefix pre-filter (built this session) to scope-based filtering in vectorSearch adapter.

### 1.4 100-Question Benchmark

Complete the 100-question LongMemEval run (currently in progress).

Target: >60% accuracy (baseline v68: 50%, code-only: 40%, hybrid 10Q: 100%).

### Completion Criteria

- [ ] `ingestConversation` hybrid mode works in mama-core API
- [ ] `ingestDocument` API works
- [ ] Memory agent extracts facts from live Claude Code session
- [ ] Scope-based vector search verified
- [ ] 100-question benchmark completed, accuracy >60%

---

## Phase 2: v0.17 — Connector Framework + Messengers

**Goal:** Apps can register with MAMA OS and feed data to mama-core.

### 2.1 platform-core Integration

Migrate from mama-suite into MAMA OS:

- `AppManifest` — app registration contract
- `ProcessSupervisor` — lifecycle management (start/stop/restart/health)
- `AppRegistry` — registered app catalog
- `AppClient` — inter-app HTTP communication

### 2.2 mama app CLI

```bash
mama app register ./slack-connector   # read manifest, register
mama app start slack                  # supervisor starts process
mama app stop slack                   # graceful shutdown
mama app list                         # status table
mama app logs slack                   # tail logs
mama app health                       # all apps health check
```

### 2.3 Connector Contract

Every connector follows this standard:

```
Required:
  /health          → { status, lastSync, messageCount }
  /manifest        → AppManifest (name, capabilities, scope, raw_archive_path)

Behavior:
  On start:    Register scope with mama-core
  Running:     Delta polling → raw archive → domain extraction agent → structured units → MAMA OS ingest API
  On stop:     Graceful cleanup, flush pending extractions

Intelligence distribution:
  Connector:   Domain-specialized extraction (Tier 1)
               - Kagemusha: message thread analysis, feedback detection
               - GWS: email→decision mapping, calendar→constraint extraction
               - Claude Code: code decision + architecture choice extraction
  MAMA OS:     Cross-connector reasoning (Tier 2)
               - Pattern detection across connectors
               - Contradiction resolution
               - Temporal analysis

Raw data stays in connector. MAMA OS stores structured units + evidence pointers.
```

### 2.4 Official Connectors (Messenger-Native)

Messengers are MAMA OS's unique strength. No competitor has these.

**Slack Connector:**

- Bot Token authentication
- Channel polling → `ingestConversation`
- File attachments → `ingestDocument`
- Thread support (thread = session)

**Telegram Connector:**

- Bot Token authentication
- Chat/group polling → `ingestConversation`
- Image/file → `ingestDocument`
- Bidirectional: MAMA OS can send alerts via Telegram

### 2.5 Existing Entry Point Integration

Unify existing MAMA ecosystem entry points:

```
Claude Code Plugin → scope: "claude-code/{project}"
MCP Server         → scope: "mcp-server/{session}"
```

Both already save to mama-core. Add proper scope tagging so cross-source search works.

### 2.6 MCP Server Bridge

For external apps (Gmail, Calendar, GitHub, Notion), do NOT build custom connectors. Instead, bridge existing MCP servers:

```
[Gmail MCP Server] → [MCP Bridge] → mama.ingestConversation()
[Calendar MCP]     → [MCP Bridge] → mama.ingestDocument()
[GitHub MCP]       → [MCP Bridge] → mama.ingestConversation()
```

The bridge is a thin adapter: reads MCP server output, converts to mama-core ingest format. Leverages the 100+ existing MCP servers without reinventing connectors.

### Completion Criteria

- [ ] `mama app list` shows registered connectors
- [ ] Slack connector: channel messages stored as facts in mama-core
- [ ] Telegram connector: bidirectional (ingest + alerts)
- [ ] `mama memory search "meeting"` returns Slack facts
- [ ] Claude Code Plugin facts searchable alongside connector facts
- [ ] MCP Bridge prototype working with at least one MCP server

---

## Phase 3: v0.18 — Control Tower UI

**Goal:** Users see their connected digital life in one place.

### 3.1 Remove Deprecated Features

Delete from codebase:

- Skills marketplace tab
- Playground tab
- Agent Swarm section (Dashboard)
- MCP Servers section (Dashboard)

### 3.2 Control Tower (Main Screen)

The main screen answers: "What is happening across my apps right now?"

```
┌──────────────────────────────────────────────┐
│  MAMA OS                     ✓ Healthy  4d   │
├──────────────────────────────────────────────┤
│                                              │
│  Event Stream              Connected Apps    │
│  ┌────────────────────┐    ✓ Slack           │
│  │ 14:23 slack         │    ✓ Telegram       │
│  │  "meeting Wed 2pm"  │    ✓ Claude Code    │
│  │ 14:15 telegram      │                     │
│  │  "sending docs"     │    Memory           │
│  │ 14:08 claude-code   │    1,247 facts      │
│  │  "chose JWT auth"   │    23 new today     │
│  │                     │                     │
│  │ Connected context:  │    Health: 98/100   │
│  │ "Project X meeting  │                     │
│  │  — 3 apps, related" │                     │
│  └────────────────────┘                      │
└──────────────────────────────────────────────┘
```

Key: event stream shows mama-core's **connected context**, not raw messages. "Slack meeting + Telegram docs + Gmail attachment = Project X meeting" appears as one linked item.

### 3.3 Memory Explorer

- Graph visualization (evolve current Graph tab)
- Search with scope filters (by app, time range)
- Timeline view: evolution of a topic over time
- Fact detail: source app, supersedes chain, related facts

### 3.4 Settings (Simplified)

- Connector management (connect/disconnect via OAuth)
- Gateway settings (Discord, Slack, Telegram bots)
- Basic agent config (model, timeouts)
- Security (API tokens, audit log viewer)

### Completion Criteria

- [ ] Skills, Playground, Agent Swarm removed from codebase
- [ ] Control Tower shows real-time event stream
- [ ] Memory Explorer shows cross-app search results
- [ ] Non-developer can understand "what this system does" from the UI

---

## Phase 4: v0.19 — Stability + Security

**Goal:** MAMA OS is trustworthy enough to run 24/7 unattended.

### 4.1 Watchdog

```
MAMA OS daemon monitoring:
  Self:        child process crash → auto-restart (5s delay, max 3 retries)
  Connectors:  /health every 60s → 3 failures → restart → alert if still failing
  Embedding:   port 3849 health → auto-restart on failure
```

### 4.2 Audit Log

```
~/.mama/audit.log (local only, never transmitted)

Logged events:
  [memory] save/search/delete operations
  [app] connector start/stop/health/error
  [api] external API calls with auth status
  [security] auth failures, blocked IPs
```

Viewable in Control Tower UI under Security tab.

### 4.3 Graceful Restart

```
mama restart:
  1. Start new process
  2. Verify mama-core DB connection
  3. Notify connectors
  4. Terminate old process
  5. New process reconnects connectors
  → Zero memory loss, minimal session interruption
```

### 4.4 Health Score

```
mama status:
  Health Score: 98/100
    mama-core DB:       ✓ (12ms)
    Embedding server:   ✓ (45ms)
    Slack connector:    ✓ (last sync 2m ago)
    Telegram connector: ✓ (last sync 30s ago)
    Disk:               ✓ (45MB / 10GB free)
    Uptime:             ✓ (4d 12h, 0 restarts)
```

Deductions: connector down (-10), DB slow (-5), disk low (-15), recent crash (-20).

### Completion Criteria

- [ ] Watchdog auto-recovers connector crash within 10s
- [ ] Audit log records all mama-core operations
- [ ] `mama status` shows health score
- [ ] 72-hour unattended uptime test passes

---

## Phase 5: v0.20 — Onboarding + Connector Expansion

**Goal:** Non-developers can set up MAMA OS in 5 minutes.

### 5.1 Browser-Based Onboarding

```
npx @jungjaehoon/mama-os
→ Browser opens:

Step 1: Choose apps to connect
  [Slack ✓] [Telegram] [Gmail] [Calendar]

Step 2: Sign in
  "Sign in to Slack" [OAuth button]

Step 3: Done
  "MAMA OS is collecting and connecting your data.
   Open the Control Tower to see what's happening."
```

No terminal knowledge required. App selection → OAuth → done.

### 5.2 Additional Messenger Connectors

- Discord connector (bot token, guild/channel support)

### 5.3 MCP Bridge Expansion

Leverage MCP server ecosystem for:

- Gmail (existing MCP server → bridge → mama-core)
- Google Calendar (existing MCP server → bridge → mama-core)
- GitHub (existing MCP server → bridge → mama-core)
- Notion (existing MCP server → bridge → mama-core)

MAMA OS does NOT build these connectors. It bridges existing MCP servers through a standard adapter.

### Completion Criteria

- [ ] Non-developer completes setup in <5 minutes via browser
- [ ] 3+ messenger connectors working (Slack, Telegram, Discord)
- [ ] MCP Bridge connects 2+ external MCP servers to mama-core
- [ ] Cross-app search returns results from all connected sources

---

## Phase 6: v1.0 — General Release

**Goal:** Ship with confidence.

### 6.1 Release Checklist

```
Memory Quality:
  ✓ 100-question benchmark >60% accuracy
  ✓ Cross-app search accuracy verified
  ✓ Supersedes evolution chains working correctly

App Ecosystem:
  ✓ 3+ connectors working (Slack, Telegram, Discord)
  ✓ MCP Bridge working with 2+ MCP servers
  ✓ mama app CLI fully operational
  ✓ Connector development guide published

Stability:
  ✓ 72-hour unattended uptime test
  ✓ Watchdog auto-recovery verified
  ✓ Audit log operational
  ✓ Health score accurate

Usability:
  ✓ Non-developer install test (<5 minutes)
  ✓ Onboarding flow complete
  ✓ Control Tower UI complete

Documentation:
  ✓ README (install + quickstart)
  ✓ Connector development guide (third-party)
  ✓ mama-core API reference
  ✓ Vision document (exists)
```

### 6.2 Positioning

```
npm: npx @jungjaehoon/mama-os
Tagline: "Your digital life, remembered locally"

One-liner: Local AI runtime that connects your apps,
remembers what matters, and keeps it all on your device.
```

### 6.3 Third-Party Connector Guide

Enable community connectors:

```
1. Write AppManifest
2. Implement /health endpoint
3. Collect data → mama.ingestConversation() or ingestDocument()
4. mama app register
→ MAMA OS handles lifecycle, health, UI
```

---

## Milestone Summary

| Version   | Name                   | Key Deliverable                                                                        | Depends On |
| --------- | ---------------------- | -------------------------------------------------------------------------------------- | ---------- |
| v0.15     | Memory Quality Proof   | 10Q benchmark 100%                                                                     | ✅ Done    |
| **v0.16** | Memory Engine          | ingestConversation hybrid, ingestDocument, memory agent, scoped search, 100Q benchmark | —          |
| **v0.17** | Connector Framework    | platform-core, mama app CLI, Slack+Telegram connectors, MCP Bridge                     | v0.16      |
| **v0.18** | Control Tower UI       | Event stream, Memory Explorer, remove deprecated tabs                                  | v0.17      |
| **v0.19** | Stability              | Watchdog, audit log, health score, graceful restart                                    | v0.18      |
| **v0.20** | Onboarding + Expansion | Browser setup, Discord connector, MCP Bridge expansion                                 | v0.19      |
| **v1.0**  | General Release        | Quality assurance, docs, 72h uptime test                                               | v0.20      |

Each version is an independent npm release. Users can adopt at any milestone.

---

## Risks and Mitigations

| Risk                                                     | Impact                                        | Mitigation                                                            |
| -------------------------------------------------------- | --------------------------------------------- | --------------------------------------------------------------------- |
| Supermemory dominates cloud memory market                | Reduces MAMA OS's appeal to cloud-first users | Double down on local-only positioning. Different market.              |
| MCP Bridge proves fragile with third-party MCP servers   | Connector expansion blocked                   | Build official connectors as fallback for critical apps               |
| Hybrid extraction Sonnet costs at scale                  | 100+ questions expensive                      | Expand regex coverage to reduce LLM calls. Batch optimization (done). |
| Non-developer onboarding complexity (OAuth, permissions) | Adoption barrier                              | Invest in guided setup wizard. Test with real non-developers.         |
| mama-core DB grows too large on disk                     | Performance degradation                       | Implement fact TTL, archival, and compaction policies                 |

---

## Design Decisions Log

| Decision                                     | Rationale                                                                                                                                                                                                                                                                            |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Local-only, no cloud option                  | Privacy as core value. Differentiates from Supermemory/Mem0.                                                                                                                                                                                                                         |
| MCP Bridge over custom connectors            | 100+ MCP servers exist. Building Gmail/Calendar/GitHub from scratch is wheel reinvention.                                                                                                                                                                                            |
| Messenger connectors built in-house          | No MCP servers for Kakao/LINE. Messenger-native is MAMA OS's unique moat.                                                                                                                                                                                                            |
| Remove Skills/Playground/Agent Swarm         | Commoditized by Claude. Focus on what Claude cannot do.                                                                                                                                                                                                                              |
| Milestone-based releases                     | Each version independently useful. No big-bang 1.0 risk.                                                                                                                                                                                                                             |
| Memory-first roadmap                         | mama-core quality determines everything downstream. Kagemusha experience confirmed this.                                                                                                                                                                                             |
| MAMA OS runtime, not just MCP server         | MCP is a tool Claude invokes on demand. MAMA OS is an always-on runtime that receives hook events, runs memory agent, manages connectors, and provides context continuously. This is the core architectural reason MAMA OS must exist.                                               |
| Memory agent via hooks, not terminal polling | Claude Code Plugin hooks (UserPromptSubmit, PostToolUse) already fire on every interaction. MAMA OS receives these via HTTP. No terminal scraping or polling needed. Graceful degradation when MAMA OS is off.                                                                       |
| Connector-Orchestrator over Data Lake        | Raw data stays in connectors, not copied to MAMA OS. Connectors have domain-specific extraction agents (Tier 1). MAMA OS reasons across all connectors (Tier 2). Evidence pointers reference raw when verification needed. Avoids noise in Knowledge Graph, respects data ownership. |
| 2-tier intelligence distribution             | Connectors = domain experts (email parsing, code analysis, message thread analysis). MAMA OS = strategist (cross-connector patterns, temporal analysis, contradiction resolution). Kagemusha's delta-only model proved that single-tier analysis lacks cross-channel insight.        |
| Transcript-based capture over stdin fields   | Claude Code's UserPromptSubmit stdin only contains `prompt` (no assistant response). But `transcript_path` provides full JSONL history. Reading transcript gives complete user+assistant context per turn.                                                                           |
| PostToolUse mama_save → MAMA OS relay        | Plugin MCP saves to ~/.claude/ (connector local DB). PostToolUse detects mama_save tool use and relays the decision to MAMA OS, keeping both DBs in sync without bidirectional replication.                                                                                          |
