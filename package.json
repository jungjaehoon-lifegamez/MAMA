{
  "name": "claude-mama",
  "version": "1.0.0",
  "private": true,
  "description": "Memory-Augmented MCP Assistant (MAMA) for Claude Code & Desktop - Monorepo",
  "repository": {
    "type": "git",
    "url": "https://github.com/jungjaehoon-lifegamez/MAMA.git"
  },
  "homepage": "https://github.com/jungjaehoon-lifegamez/MAMA#readme",
  "bugs": {
    "url": "https://github.com/jungjaehoon-lifegamez/MAMA/issues"
  },
  "keywords": [
    "claude-code",
    "claude-desktop",
    "mcp-server",
    "memory-assistant",
    "monorepo"
  ],
  "author": "SpineLift Team",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint packages/*/src packages/*/tests --ext .js,.mjs",
    "lint:fix": "eslint packages/*/src packages/*/tests --ext .js,.mjs --fix",
    "format": "prettier --write \"packages/*/{src,tests}/**/*.{js,mjs,json,md}\"",
    "format:check": "prettier --check \"packages/*/{src,tests}/**/*.{js,mjs,json,md}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.0",
    "vitest": "^1.0.0"
  },
  "lint-staged": {
    "*.{js,mjs}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
